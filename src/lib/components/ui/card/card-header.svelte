<script lang="ts">
	import { cn, type WithElementRef } from '$lib/utils.js';
	import type { HTMLAttributes } from 'svelte/elements';

	let {
		ref = $bindable(null),
		class: className,
		children,
		...restProps
	}: WithElementRef<HTMLAttributes<HTMLDivElement>> = $props();
</script>

<div
	bind:this={ref}
	data-slot="card-header"
	class={cn(
		'@container/card-header bg-secondary grid auto-rows-min grid-rows-[auto_auto] items-start gap-1.5 border-b px-6 py-4 last:border-b-0 has-data-[slot=card-action]:grid-cols-[1fr_auto]',
		className
	)}
	{...restProps}
>
	{@render children?.()}
</div>
